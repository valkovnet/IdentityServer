@if (Request.IsAuthenticated) {
    <p>
        Hello, @User.Identity.Name!
        @Html.ActionLink("Sign out", "SignOut", "Account")
    </p>
} else {
    <ul>
        <li>anonymous</li>
        @Html.ActionLink("Identity", "Index", "Account")
    </ul>
}